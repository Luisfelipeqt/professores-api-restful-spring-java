spring:
  datasource:
    username: root
    password: 99620530
    url: jdbc:mysql://localhost:3306/hyperprof?useSSL=false&serverTimezone=UTC
  jpa:
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true